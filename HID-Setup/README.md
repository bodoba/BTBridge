# Setup the USB Device

The script in here enable two USB devices

/dev/hidg0 -> Keyboard
/dev/hidg1 -> Mouse

and bind these to the OTG controller

The install.sh enable this service persistent over boottime.

Tested on Armbian 26.8.3 in a Orange PI PC Plus
